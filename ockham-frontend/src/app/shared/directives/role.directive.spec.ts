import { RoleDirective } from 'src/app/shared/directives/role.directive';

describe('RoleDirective', () => {
  it('should create an instance', () => {
    const directive = new RoleDirective(null, null, null);
    expect(directive).toBeTruthy();
  });
});
